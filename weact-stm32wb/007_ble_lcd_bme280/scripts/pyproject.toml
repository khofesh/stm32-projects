[project]
name = "scripts"
version = "0.1.0"
description = "Host side helpers for the WeAct STM32WB55 + BME280 board"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bleak>=0.22",
]
